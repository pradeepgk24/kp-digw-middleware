Files in this folder are templates which are used within custom steps in step definitions.

From this templates the Base64 need to be done and then the appropriate value in custom step definition need to be updated

For example see custom_spark.json for transformers. There is attribute custom_spark_code with value which is base64 encoded token from file `transformer_custom_spark_file_template.py`