# pylint: skip-file
from middleware.common.metadatabase.model.types.difw_metadb_enum import DifwMetadbEnum


class OwnerFilterEnum(DifwMetadbEnum):
    """
    Enum for objects filter of owner
    """
    current = 'current'
